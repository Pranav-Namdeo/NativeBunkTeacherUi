
  # Create Teacher Profile UI

  This is a code bundle for Create Teacher Profile UI. The original project is available at https://www.figma.com/design/LpyH97MEIEZpXQ2X1R2fUE/Create-Teacher-Profile-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  